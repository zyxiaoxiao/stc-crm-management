<template>
	<el-auto-resizer>
		<template #default="{ height, width }">
			<!-- <component :is="treeTableV2"></component> -->
			<el-table-v2
				ref="tableV2Ref"
				:expand-column-key="props.expandColumnKey"
				:default-expanded-row-keys="props.defaultExpandedRowKeys"
				:columns="props.columns"
				:data="props.treeData"
				:width="width"
				:height="height"
				:fixed="true"
				:header-height="32"
				:row-height="32"
			/>
		</template>
	</el-auto-resizer>
</template>

<script lang="jsx" setup>
import { ref } from "vue";
import { ElCheckbox } from "element-plus";

console.log("进来了--------");
// 父组件传入的参数
const props = defineProps({
	expandColumnKey: "", //那个字段是树形结构
	defaultExpandedRowKeys: Object, //展开树形结构选中的行
	columns: Object, //列
	treeData: Object //数据源
	// rowClass: () => {} //把选中的行字体颜色设置为蓝色
});

console.log("props", props);

const treeTableV2Ref = ref();
const treeTableV2 = () => {
	//return <ElCheckbox label={"www"} modelValue={true} />;
	return (
		<el-table-v2
			ref={treeTableV2Ref}
			expand-column-key={props.expandColumnKey}
			default-expanded-row-keys={props.defaultExpandedRowKeys}
			columns={props.columns}
			data={props.treeData}
			width={700}
			height={700}
			fixed={true}
			header-height={32}
			row-height={32}
		/>
	);
};
</script>
